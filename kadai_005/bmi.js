// 変数の宣言
let weight;

// 値の代入
weight = 68;

// コンソールへの出力
console.log(weight);

// 変数の宣言
let height;

// 値の代入
height = 1.7;

// コンソールへの出力
console.log(height);

// 変数の宣言
let bmi;

// 値の代入
bmi = ((weight) / (height*height));

// コンソールへの出力
console.log(bmi);

// 四則演算
console.log( (weight) / (height*height));